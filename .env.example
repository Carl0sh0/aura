# Copy this file to ".env" and add your key to enable the real Claude AI.
# Without a key, Aura runs in a realistic "demo" mode so you can try everything.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: port for the backend API (defaults to 8787)
PORT=8787
